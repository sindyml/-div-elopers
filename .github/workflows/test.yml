name: Tests & Coverage

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests with coverage
        run: npx vitest run --coverage

      - name: Show coverage summary in GitHub Actions
        run: npx vitest run --coverage --reporter=text

      - name: Add coverage header to GitHub Summary
        run: echo "## 📊 Coverage Report" >> $GITHUB_STEP_SUMMARY

      - name: Append coverage summary to GitHub Summary
        run: |
          echo '```' >> $GITHUB_STEP_SUMMARY
          npx vitest run --coverage --reporter=text >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/